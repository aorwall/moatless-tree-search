[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"


[project]
authors = [
    {name = "Albert Örwall", email = "albert@moatless.ai"},
    {name = "Antonis Antoniades", email = "antonis@ucsb.edu"},
]
requires-python = "<3.13,>=3.10"
dependencies = [
    "moatless @ git+https://github.com/aorwall/moatless-tools.git@async",
]
name = "moatless-tree-search"
version = "0.0.4"
description = ""
readme = "README.md"

[project.scripts]
moatless-streamlit = "moatless.streamlit.cli:main_run"
moatless-evaluate = "moatless.benchmark.run_evaluation:main"

[dependency-groups]
dev = [
    "pytest<9.0.0,>=8.3.2",
    "pytest-mock<4.0.0,>=3.14.0",
    "python-dotenv<2.0.0,>=1.0.1",
    "mypy<2.0.0,>=1.10.0",
    "ruff<1.0.0,>=0.5.5",
    "coverage<8.0.0,>=7.6.1",
]
[tool.ruff]
target-version = "py310"
extend-exclude = [ "tests", "evaluations", "notebooks",]

[tool.ruff.lint]
select = [ "B", "DTZ", "E", "F", "I", "LOG", "N", "PLE", "SIM", "T20", "UP",]
ignore = [ "E501", "F401", "UP007" ]

[tool.pdm.dev-dependencies]
dev = [
    "pytest<9.0.0,>=8.3.2",
    "pytest-mock<4.0.0,>=3.14.0",
    "python-dotenv<2.0.0,>=1.0.1",
    "mypy<2.0.0,>=1.10.0",
    "ruff<1.0.0,>=0.5.5",
    "coverage<8.0.0,>=7.6.1",
]

[tool.pdm.build]
includes = [
    "moatless",
]
